numpy
pandas
Pillow
pytesseract  # optional system tesseract binary enables numeric OCR
scipy
scikit-image   # skeletonize (thinning, morphology); fallback erosion used if missing

# Dev / testing
pytest

# Optional: ML-assisted candidate reranking
# torch
